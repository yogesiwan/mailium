#!/usr/bin/expect -f
set timeout -1
spawn ssh -o StrictHostKeyChecking=no system-cache@143.244.128.71 "cd apps/mailium && git pull origin main && docker-compose up -d --build"
expect "*?assword:*"
send "yogesh\r"
expect eof
