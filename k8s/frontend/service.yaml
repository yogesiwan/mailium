apiVersion: v1
kind: Service
metadata:
  name: cache-router
  namespace: mailium
spec:
  type: NodePort
  selector:
    app: cache-router
  externalTrafficPolicy: Cluster
  ports:
    - name: http
      port: 8080
      targetPort: http
      nodePort: 32341
